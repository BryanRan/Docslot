<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteBase /monprojet/ # <- remplace par le nom de ton dossier si tu es en localhost RewriteCond
		%{REQUEST_FILENAME} !-f RewriteCond %{REQUEST_FILENAME} !-d RewriteRule ^(.*)$ index.php/$1 [L] </IfModule>